// TODO: Disable for browser.
export default function logCallsite() {
  const oldLog = console.log
  console.log = (...args) => {
    oldLog(...args)
    oldLog(require('callsite')()[1].getFileName())
  }
}
