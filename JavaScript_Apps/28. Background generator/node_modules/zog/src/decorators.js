import {wrap} from 'decorator-wrap'

function _logFn(callback, args, name, type) {
  console.log('Starting  ', type, name)
  var result = callback()
  console.log('Ended: ', name)
  return result
}

export function logFn(...args) { return wrap(logFn)(...args) }
