
describe('Module', function() {

  //beforeEach(function () {
  //  this.sinon = sinon.sandbox.create()
  //  this.sinon.stub(console, 'log')
  //})
  //
  //afterEach(function() {
  //  this.sinon.restore()
  //})
  //
  //it('should work', function() {
  //  log.debug('hello')
  //  expect(console.log.calledOnce).to.be.true
  //})

  it('should work', function() {

    process.env.DEBUG = '*'

    {
      const {log, warn, debug, error} = require('..').logger.ns('live')
      log('log')
      warn('warn')
      debug('debug')
      error('error')
    }

    const log2 = require('..')
    log2.debug('This is a test')
    log2.log('Yo')

    const {log, warn, debug, error} = require('..').default
    log('log')
    warn('warn')
    debug('debug')
    error('error')

  })

})
