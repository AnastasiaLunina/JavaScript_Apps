'use strict';

describe('Module', function () {

  //beforeEach(function () {
  //  this.sinon = sinon.sandbox.create()
  //  this.sinon.stub(console, 'log')
  //})
  //
  //afterEach(function() {
  //  this.sinon.restore()
  //})
  //
  //it('should work', function() {
  //  log.debug('hello')
  //  expect(console.log.calledOnce).to.be.true
  //})

  it('should work', function () {

    process.env.DEBUG = '*';

    {
      var _require$logger$ns = require('..').logger.ns('live');

      var _log = _require$logger$ns.log;
      var _warn = _require$logger$ns.warn;
      var _debug = _require$logger$ns.debug;
      var _error = _require$logger$ns.error;

      _log('log');
      _warn('warn');
      _debug('debug');
      _error('error');
    }

    var log2 = require('..');
    log2.debug('This is a test');
    log2.log('Yo');

    var _require$default = require('..').default;

    var log = _require$default.log;
    var warn = _require$default.warn;
    var debug = _require$default.debug;
    var error = _require$default.error;

    log('log');
    warn('warn');
    debug('debug');
    error('error');
  });
});
//# sourceMappingURL=index.spec.js.map
