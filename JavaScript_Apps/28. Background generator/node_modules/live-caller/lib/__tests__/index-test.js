'use strict';

var liveCaller = require('..');

describe('Module', function () {

  it.skip('.getIDELink', function () {
    var actual = void 0;

    function test() {
      actual = liveCaller.getIDELink(1, { color: false });
    }

    test();
    var expected = 'idea://open?file=' + __filename + '&line=16:5';
    expect(actual).to.equal(expected);
  });

  it.skip('.getCallerFile', function () {
    // Extremely brittle test.
    var actual = liveCaller.getCallerFile(0);
    var expected = {
      path: __filename,
      line: 22,
      col: 29
    };
    expect(actual).to.eql(expected);
  });
});
//# sourceMappingURL=index-test.js.map
