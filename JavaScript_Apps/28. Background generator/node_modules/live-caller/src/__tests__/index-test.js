const liveCaller = require('..')

describe('Module', function() {

  it.skip('.getIDELink', () => {
    let actual

    function test() {
      actual = liveCaller.getIDELink(1, {color: false})
    }

    test()
    const expected = `idea://open?file=${__filename}&line=16:5`
    expect(actual).to.equal(expected)
  })

  it.skip('.getCallerFile', () => {
    // Extremely brittle test.
    let actual = liveCaller.getCallerFile(0)
    const expected = {
      path: __filename,
      line: 22,
      col: 29,
    }
    expect(actual).to.eql(expected)
  })

})
