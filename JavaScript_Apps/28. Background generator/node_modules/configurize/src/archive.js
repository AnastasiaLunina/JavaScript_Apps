
//const observers = {}

// TODO(vjpr): Use event-kit observables.
//main.observe = (key, cb) => {
//  observers[key] = cb
//}

//_notifyObservers(key, newValue) {
//  const prevVal = config[key]
//  const fn = observers[key]
//  if (fn) fn(newValue, prevVal)
//}

// TODO(vjpr)
//main.setSchema = function(namespace, schema) {
//  globalSchema.properties = _.merge(globalSchema.properties, {[namespace]: schema})
//  const ajv = require('ajv')
//  const validate = ajv.compile(schema)
//  return _.defaultsDeep(config, validate({}))
//}
